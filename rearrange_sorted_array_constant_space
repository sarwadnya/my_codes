int max_idx=n-1,min_idx=0,max_el=arr[max_idx]+1;
	for(int i=0;i<n;i++)
	    if(i%2==0)
	        arr[i]+= (arr[max_idx--]%max_el) *max_el;
	    else
	        arr[i] += (arr[min_idx++]%max_el)*max_el;
	for(int i=0;i<n;i++)
	    arr[i] /= max_el;
